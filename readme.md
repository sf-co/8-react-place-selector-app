# Git

```
echo "# 8-react-place-selector-app" >> README.md
git init
git add README.md
git commit -m "first commit"
git branch -M main
git remote add origin https://github.com/sf-co/8-react-place-selector-app.git
git push -u origin main

```
